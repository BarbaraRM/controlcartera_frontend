import { FilterProdSucPipe } from './filter-prod-suc.pipe';

describe('FilterProdSucPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProdSucPipe();
    expect(pipe).toBeTruthy();
  });
});
